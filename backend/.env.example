DATABASE_URL=postgres://rental:rental@localhost:5433/rental
JWT_SECRET=change-me-to-a-long-random-string
CORS_ORIGIN=http://localhost:5174
PORT=4001

# Email verification (Resend). Leave blank for dev-mode console logging.
RESEND_API_KEY=
RESEND_FROM=AG Rental <onboarding@resend.dev>

# Phone verification (Twilio Verify). Leave blank for dev mode
# (code "123456" will always succeed).
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_VERIFY_SERVICE_SID=
